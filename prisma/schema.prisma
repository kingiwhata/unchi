generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_PRISMA_URL")
}

model Post {
    id        String   @id @default(cuid())
    date      DateTime
    updatedAt DateTime
    stickers  String[]
    notes     String
}
